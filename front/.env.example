# 개발 서버에서는 /api, /sourcing 경로를 Vite proxy가 로컬 백엔드로 전달합니다.
VITE_API_BASE_URL=/api
VITE_OAUTH_BASE_URL=
VITE_API_PROXY_TARGET=http://localhost:8080

# JWT는 로그인 후 HttpOnly 쿠키로 내려오며, 프론트는 사용자 ID 헤더를 보내지 않습니다.
